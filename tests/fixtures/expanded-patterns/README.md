Expanded-pattern fixture root used by feature 002 tests.

Quickstart and automated validation create equivalent category layouts through
`tests.support.fixture_builders` under a temporary workspace.

Planned validation categories:

- `home/.ssh` via `HOME`
- `repo-keys`
- `config-secrets`
- `infra`
- `vpn`
- `noise`